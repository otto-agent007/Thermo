name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      JAX_PLATFORMS: cpu
      PYTHONHASHSEED: "0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Install frozen environment
        run: uv sync --frozen
      - name: Check lockfile is current
        run: uv lock --check
      - name: Check formatting
        run: uv run ruff format --check .
      - name: Lint
        run: uv run ruff check .
      - name: Test
        run: uv run pytest -m "not slow"
      - name: Build package artifacts
        run: uv build
      - name: Verify target-context config in package artifacts
        run: |
          python - <<'PY'
          import glob
          import tarfile
          import zipfile

          config = "configs/experiments/thrml-target-context-pasym-swap.toml"
          wheel, = glob.glob("dist/thermo_lab-*.whl")
          sdist, = glob.glob("dist/thermo_lab-*.tar.gz")
          with zipfile.ZipFile(wheel) as archive:
              assert any(name.endswith(config) for name in archive.namelist())
          with tarfile.open(sdist) as archive:
              assert any(name.endswith(config) for name in archive.getnames())
          PY
      - name: Run cross-library smoke experiment
        run: uv run thermo-lab smoke --output-dir "${RUNNER_TEMP}/thermo-smoke"
      - name: Run weighted graph-walk baseline
        run: >-
          uv run thermo-lab run
          configs/experiments/torx-weighted-graph-walk.toml
          --output-dir "${RUNNER_TEMP}/weighted-graph-walk"
      - name: Run independent PAsymSwap compiler baseline
        run: >-
          uv run thermo-lab run
          configs/experiments/thrml-independent-pasym-swap.toml
          --seeds 0,1,2
          --output-dir "${RUNNER_TEMP}/independent-pasym-swap"
      - name: Run exact target-context PAsymSwap compiler study
        run: >-
          uv run thermo-lab run
          configs/experiments/thrml-target-context-pasym-swap.toml
          --seeds 0,1,2
          --output-dir "${RUNNER_TEMP}/target-context-pasym-swap"
